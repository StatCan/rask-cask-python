from setuptools import setup, find_packages

setup(name='rask_cask',
      author='Diego Ripley',
      author_email='diego.ripley@canada.ca',
      version='0.1',
      packages=find_packages(),
      include_package_data=True
      )
